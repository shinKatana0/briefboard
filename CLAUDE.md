# CLAUDE.md

Project rules are in `AGENTS.md`. Read it in full before you start working.

## Claude Code specifics
If you are the orchestrator in Claude Code — the worker subagent to which task
implementation is delegated is defined in `.claude/agents/worker.md` and follows
`agents/WORKER.md`.
