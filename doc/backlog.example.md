# Backlog

<!-- Managed by agents. Format: agents/PROTOCOL.md. Prefer writing via tools/task.mjs -->

## T-0001 · Major · Example task, so the shape is visible
- type: feature
- status: open
- created: 2026-01-01 09:00:00
- closed: —
- briefs: 
- labels: ui, docs

Free-form description in markdown. It ends at the next `## T-NNNN` header.

The `labels` line is optional and carries the labels this task happens to have.
Nothing declares them: a label exists while some task carries it (agents/PROTOCOL.md).
