## Summary

Briefly describe what this PR changes and why.

## Related task

Link the task this PR implements (e.g. `T-0007`) and its brief in `doc/brief/`.
Changes should stay within the brief's scope.

## Checklist

- [ ] Branch is named `task/T-NNNN-short-slug`.
- [ ] Changes match the linked brief's acceptance criteria.
- [ ] `node --test` passes locally.
- [ ] `node tools/task.mjs validate` passes.
- [ ] No new runtime dependencies were added.
- [ ] Comments and docs are in English.
